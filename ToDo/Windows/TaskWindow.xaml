<Window x:Class="ToDo.Windows.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDo.Windows"
        xmlns:local0="clr-namespace:ToDo"
        xmlns:local1="clr-namespace:ToDo.Helpers"
        mc:Ignorable="d" Title="Задача" 
        Height="480" Width="520"
        ResizeMode="NoResize">

    <Window.Resources>
        <local1:PriorityConverter x:Key="PrConv" />
        <local1:StatusConverter x:Key="StConv" />
    </Window.Resources>

    <Window.CommandBindings>
        <CommandBinding Command="local0:WindowCommands.TaskOk" Executed="BtnOk_Click"/>
        <CommandBinding Command="local0:WindowCommands.TaskCancel" Executed="BtnCancel_Click"/>
    </Window.CommandBindings>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название задачи:" Margin="0 0 0 4"/>
        <TextBox Grid.Row="1" x:Name="NameField" Height="28" Margin="0 0 0 12"/>

        <TextBlock Grid.Row="2" Text="Описание:" Margin="0 0 0 4"/>
        <TextBox Grid.Row="3" x:Name="DescriptionField" Height="110" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Margin="0 0 0 12"/>

        <Grid Grid.Row="4" Margin="0 0 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Приоритет:" Margin="0 0 0 4"/>
                <ComboBox x:Name="PriorityField" Height="28">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource PrConv}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Статус:" Margin="0 0 0 4"/>
                <ComboBox x:Name="StatusField" Height="28">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource StConv}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="5" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Срок выполнения:" Margin="0 0 0 4"/>
                <DatePicker x:Name="DateField" Height="28" Width="180" HorizontalAlignment="Left"/>
            </StackPanel>

            <CheckBox Grid.Column="1" x:Name="ImportantField" 
                      Content="Важная задача" 
                      VerticalAlignment="Bottom"
                      Margin="30 0 0 8"/>
        </Grid>

        <TextBlock Grid.Row="6" x:Name="ErrorField" 
                   Foreground="Red" FontWeight="SemiBold" TextWrapping="Wrap"
                   Margin="0 4 0 8" Visibility="Collapsed"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="local0:WindowCommands.TaskOk" Content="ОК" Width="100" Height="32" Margin="0 0 12 0"/>
            <Button Command="local0:WindowCommands.TaskCancel" Content="Отмена" Width="100" Height="32"/>
        </StackPanel>
    </Grid>
</Window>